import type{NextRequest}from"next/server";import{authenticatedProxy}from"@/lib/server/authenticated-proxy";async function forward(r:NextRequest,c:{params:Promise<{path:string[]}>}){const{path}=await c.params;return authenticatedProxy(r,`/invitations/${path.join("/")}${r.nextUrl.search}`)};export const GET=forward;export const POST=forward
