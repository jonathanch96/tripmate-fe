"use client";import{createContext,useContext,type ReactNode}from"react";import type{Participant,Trip}from"@/features/trip/types";const Context=createContext<{trip:Trip;participants:Participant[]}|null>(null);export function TripProvider({trip,participants,children}:{trip:Trip;participants:Participant[];children:ReactNode}){return <Context.Provider value={{trip,participants}}>{children}</Context.Provider>}export function useTrip(){const value=useContext(Context);if(!value)throw new Error("TripProvider is missing");return value}
