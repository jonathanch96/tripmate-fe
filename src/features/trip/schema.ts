import{z}from"zod";export const tripSchema=z.object({name:z.string().trim().min(1).max(160),baseCurrency:z.string().length(3).transform(v=>v.toUpperCase()),startDate:z.string().min(1),endDate:z.string().min(1),editPermission:z.enum(["everyone","own_only"]),approvalRequiredExpenses:z.boolean(),approvalRequiredSettlements:z.boolean(),multiCurrencyEnabled:z.boolean(),allowSettlementBeforeEnd:z.boolean()}).refine(v=>v.endDate>=v.startDate,{path:["endDate"],message:"End date must be on or after start date"});export type TripInput=z.infer<typeof tripSchema>
